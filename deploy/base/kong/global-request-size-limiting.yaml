apiVersion: configuration.konghq.com/v1
kind: KongClusterPlugin
metadata:
  name: global-request-size-limiting
  annotations:
    kubernetes.io/ingress.class: kong
  labels:
    global: "true"
plugin: request-size-limiting
config:
  allowed_payload_size: 5
  size_unit: megabytes